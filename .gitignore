# ─────────────────────────────────────────────────────────────────────────────
# NO Granola code or binaries belong in this repository.
# The app is proprietary: each user extracts their own copy via scripts/extract.sh.
# This repo holds ONLY our own code (stubs, scripts, documentation).
# ─────────────────────────────────────────────────────────────────────────────
*.dmg
*.asar
*.node
# workspace: extracted app, unpacked asar, local native builds
# (gitignore has no inline comments: a trailing "# ..." on a pattern line
# becomes part of the pattern and silently disables it)
work/
app-src/
Granola/
Granola.app/

# Node
node_modules/
package-lock.json
npm-debug.log*

# Runtime
dist/
out/
*.log
.DS_Store
